- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    # zsh
    ~/.zshrc: zsh/zshrc
    ~/.zsh_aliases: zsh/zsh_aliases
    # git
    ~/.gitignore_global: git/gitignore_global
    ~/.gitconfig: git/gitconfig

- create:
    - ~/Code
